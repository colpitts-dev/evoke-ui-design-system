import { Button } from "@evoke-ui/core/src";
import { Meta, Story, Preview, ArgsTable } from "@storybook/addon-docs/blocks";

<Meta title="Atoms/Button" component={Button} />

export const Template = ({ children, ...args }) => {
  return <Button {...args}>{children}</Button>;
};

# Button

A graphical control element that provides the user a simple way to trigger an event

## Default

<Preview>
  <Story
    name="Default"
    args={{
      theme: "primary",
      children: "Click Me!",
    }}
    argTypes={{
      theme: {
        options: ["primary", "secondary", "success", "info", "warn", "danger"],
        control: { type: "radio" },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

## Themes

<Preview>
  <Story name="Themes">
    <div className="space-x-2 mb-4">
      <Button theme="primary">Primary</Button>
      <Button theme="secondary">Secondary</Button>
    </div>
    <div className="space-x-2 mb-4">
      <Button theme="success">Success</Button>
      <Button theme="info">Information</Button>
      <Button theme="warn">Warning</Button>
      <Button theme="danger">Danger</Button>
    </div>
  </Story>
</Preview>

## Props

<ArgsTable of={ Button } />